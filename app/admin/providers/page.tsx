export default function AdminProvidersPage() {
  return (
    <section className="panel px-6 py-6 sm:px-8">
      <span className="eyebrow">Providers</span>
      <h1 className="mt-4 text-3xl font-semibold">Provider directory and licensing oversight</h1>
      <p className="mt-3 max-w-2xl text-sm leading-6 muted">
        This area is prepared for provider approvals, credentials review, specialty metadata, and organization ties.
      </p>
    </section>
  );
}
