export default function ProviderSettingsPage() {
  return (
    <section className="panel px-6 py-6 sm:px-8">
      <span className="eyebrow">Settings</span>
      <h1 className="mt-4 text-3xl font-semibold">Provider profile, licensing, and organization settings</h1>
      <p className="mt-3 max-w-2xl text-sm leading-6 muted">
        Provider-facing metadata like credentials, states served, telehealth support, and organization ties can live here.
      </p>
    </section>
  );
}
